{
  "name": "flutter",
  "version": "1.0.0",
  "description": "A new Flutter project.",
  "scripts": {
    "build:android": "flutter build apk --no-sound-null-safety",
    "build:linux": "flutter build linux --no-sound-null-safety",
    "build:windows": "flutter build windows --no-sound-null-safety"
  }
}
